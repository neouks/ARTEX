# ARTEX 提交区间审查与优化

## 范围与边界

- 北京时间 2026-09-08 14:09:00 至 2026-09-11 00:01:59，包含 00:01 这一分钟。
- Git 日期筛选命中 43 个提交；区间末端为 `0b20451`，起点为 `98992f9`。
- `98992f9^..0b20451` 的净差异涉及 231 个文件，包含合并和 vendored norma；不把 merge 与其父提交的同一改动重复计算。
- 已核对提交清单并按模块检查重点实现，深入检查了 findings 写入、冷摘要、任务授权接入、提示词及工具装配。Windows shell、重试和 UI 主要结合已有测试、静态检查及构建验证。
- 这是一轮风险驱动审查，不是对 231 个文件逐行无遗漏的证明，也不是外部渗透测试或性能压测。没有删除仅凭名字判断“像死代码”的授权和兼容逻辑。
- 改动保留在工作区，未提交、未推送；测试使用独立 PostgreSQL 库，没有使用业务库 `artex`。

## 已修复问题

### 1. 漏洞 PATCH 部分成功却返回失败（P1）

旧处理器逐字段验证并写入。提交合法 status 与非法 severity 时，返回 400，但 status 已改变；图节点同步错误又被忽略，造成两个视图不一致。

现在先验证所有字段，再通过 `DB.PatchFinding` 在同一事务内更新 findings 和图节点。图节点更新失败则整笔回滚。保留明确空字符串和未提供字段的区别。图内容真实变化时递增 content_version，重复提交相同内容不使摘要版本失效。漏洞人工处置状态不覆盖探索节点执行状态。

回归覆盖：混合非法请求不写入、有效多字段同步、原 payload 字段保留、相同内容不递增版本、数据库触发器模拟图写失败后整体回滚。

### 2. 重压缩失效操作失败后仍继续创建摘要（P1）

旧逻辑仅记录 SupersedeDigests 失败，仍压缩和新增摘要，可能留下新旧摘要重叠覆盖。

现在失败立即结束本轮重压缩，保留原始错误日志。删除未参与任何计算的 genBySig 映射和参数。

### 3. 压缩期间源内容变化导致摘要与成员不符（P1）

旧逻辑生成完整摘要后只剔除重新激活的成员，摘要正文却仍包含被剔除成员的结论；其他内容修改也未进行版本核验。

现在重新读取节点与图，检查成员存活状态、内容版本和实际压缩输入；发生变化便丢弃结果，不把旧正文附着到缩减后的成员集合。写入前使用新节点重新核验授权。

回归覆盖：源不变正常写入、三个成员中一个重新激活时整份丢弃、生成期间内容编辑时丢弃。此修复缩小并发窗口，但读取复核与 AddDigest 仍非同一数据库锁定快照，不能宣称彻底消除所有并发竞态。

### 4. 冷摘要目录与展开分类不一致（P2）

目录把摘要归入一个代表资产（成员关联最多，平局取较小 ID），旧 expand_index 却按任何成员关联资产匹配。

现在展开复用目录分类结果，删除重复分类代码，将原先逐摘要 NodeAssets 查询合并为目录已有的批量查询。权限仍每次重新计算，不使用可能延迟撤回生效的跨请求授权缓存。

回归覆盖：同一摘要关联两个资产时仅在目录代表资产下展开；现有跨任务封禁授权测试通过。

### 5. 提示词和无效说明（P3）

- 已有中文策略后缀不重复追加；普通无模板提示词直接返回，省去模板解析。
- 删除 Planner 对不存在的 asset_neighbors 工具的推荐。
- 删除冷摘要函数对已不存在参数的说明；测试恢复进入测试前的全局 PromptOverride。
- 未改写数据库中用户保存的自定义提示词；已有覆盖模板中的旧文字不会因此自动迁移。

## 设计反思：确有过度约束，但授权边界不应移交模型

### 应保留在框架

资产批准、封禁、删除墓碑、来源任务隔离、工具执行前检查、HTTP 出口拦截、撤销授权后立即取消 Worker、资源预算与审计，都涉及确定性安全或资源边界。提示词只能提醒，不能代替这些机制。入口校验、领取校验和出口拦截处于不同并发/执行边界，不是因为条件类似就可以删除的冗余。

### 可能过度介入 LLM

1. Planner 正文、心跳消息和末尾指令重复要求“没有 open/running 就必须新建意图”。等待审批、缺少凭证或所有合法方向耗尽时，强迫新建可能诱发重复探索。建议允许输出明确的等待/阻塞原因，由框架在审批或新证据到达时唤醒。
2. 固定要求 2–3 条路线、限定每轮四个意图属于策略偏好，不属于资产授权。应区分“建议的搜索策略”和“硬资源预算”，避免把所有启发式都写成最高优先级禁令。
3. 五层重试各有针对的故障，不应简单合并删除；但嵌套重试可能放大总体耗时，宜增加任务级累计尝试/时间预算及分层耗时观测，再决定调参。
4. 冷摘要让模型看到派生表示，必须保留原节点可展开、版本校验与授权过滤。压缩不应替代证据或成为新的授权依据。

本轮仅清理确定无效的工具提示和重复语言后缀，没有擅自改变“强制补意图”的产品语义，也没有迁移用户保存的提示词。

## 后续优化优先级（未实施）

- 冷摘要授权仍有按成员逐个取节点/授权的查询。建议按单次请求批量读取，不采用跨请求缓存。需要用大任务查询计数和 profile 定量验证，当前没有测得性能提升百分比。
- 如果需要严格的摘要写入快照一致性，应在数据库层引入版本条件写入及对应锁策略，覆盖图边/资产锚变化，避免仅增加一轮读检查就宣称原子性。
- 多层重试、心跳与审批等待统一到显式等待状态，属于调度设计变更，应单独回归，不混入死代码清理。
- Windows PowerShell 原生运行仍需 Windows 环境；macOS 测试通过不等于复现过 Windows 原问题。

## 验证结果

- 主仓库全部 Go 包逐包 `go test -count=1`：通过；各包使用独立 PostgreSQL 测试库。最后一次压缩修复后再次运行完整 agent 包：通过。
- `go vet ./...`：通过。
- 新增摘要并发、目录分类、既有跨任务摘要授权、语言幂等测试的 `go test -race`：通过（定向 race，不是全部包 race）。
- vendored norma 的 tool、harness、llm、agentcore 测试：通过。
- 前端 `npx tsc --noEmit`、`npm run build`：通过。
- 7 个相关前端文件定向 Biome：退出码 0，但保留 5 个既有 warning（cookie、3 个 any、无效抑制注释）及 14 个 CSS 类顺序 info。未将这些表述为零告警。
- `git diff --check`：通过。
- 没有运行真实 LLM 端到端任务、Windows 原生测试或负载基准；测试摘要生成使用本地模拟 Provider，不发送目标探测请求。

## 提交清单

按 Git 日期查询顺序：

```text
0b20451 18ba6b9 27f6016 4154999 cb9ef98 5cdfbef 7c10451 f351f0b
8dae851 c585b19 5327f88 64b08f5 aa12706 a50bf02 cc8e0f1 377ccc7
5cdb68a ae29b67 67787b5 775189d eefd5ee 4f088e5 8011609 d728e50
a9299d1 69e6295 074acb2 5fdf55b 709b0e7 7aacaf8 f270ca6 4fba36b
7d4223c 01f223a dc46457 b39284f 6b18845 1dffe05 3d71196 f29b37c
55274c4 213d653 98992f9
```
