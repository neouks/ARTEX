# 官方增量同步记录（2026-09-13）

## 范围和恢复点

- 官方起点：`8dae851b9b622f2ff2631f332fde9719d0b16fba`。
- 经用户提供的本地代理成功获取并锁定的官方终点：`44194a9ebc9414b5323a6b90f864703649c2d664`。
- 相对备份提交尚未合入的官方提交共 51 个；使用三方合并保留历史，没有重复 cherry-pick。
- 本地备份提交：`b764135e0bb6a292e1f6b5263a962d177016f1bd`。
- 备份分支：`codex/backup-before-upstream-sync-20260913`。
- 同步分支：`codex/sync-upstream-20260913`；验证通过后快进本地 main，不推送。

## 功能对照和冲突处理

| 功能 | 官方变化 | 本次处理 |
| --- | --- | --- |
| 漏洞流量证据 | 流量绑定、证据快照、报告展示和工具交接 | 接入官方记录器，保留本地写回授权、当前意图关联和 tested 规则；精简节点查询保留最多 20 个 hint 流量引用，完整节点仍按需读取 |
| 漏洞复测 | 独立复测会话、结论、修复状态及前端入口 | 接入官方流程，并为任务绑定复测设置原任务策略和代理，避免通用聊天绕过任务边界 |
| 执行审计 | 详细输入、判断及执行记录 | 同步后端、页面、Mock 和测试，保留独立 Guard |
| 多会话及分组 | 主 Agent 分段会话、按 Agent 分组、实时状态 | 同步，补齐主会话 Mock；保留本地活动合并和首次加载优化 |
| 旁路问答 | /btw 上下文快照、记忆及持久化 | 同步，并在任务问答前复用角色授权过滤；不修改原始会话审计 |
| 来源任务继承 | 创建任务支持 source_task_ids | 同步官方入口，保留本地来源只读和审批有效状态 |
| 资产 DSL | 任务内资产检索 | 页面接入 DSL，同时保留审批/测试状态筛选；工具继续使用本地授权查询实现 |
| 审批和 Worker | 本地功能 | 保留三种模板、自由录入、用户目标登记、墓碑、异常隔离；Planner 严格下发，运行中 Worker 放行 pending，主动限制仍有效 |
| 工具与上下文 | 精简说明、漏洞证据工具、SDK 更新 | 保留本地分页、搜索、响应预算、最新 Worker 输出、历史去重及管理视图；未恢复全量列表 |
| 范围和记忆 | 官方调整 related/覆盖度及 Worker 记忆 | 优先保留本地 related 参数、关闭覆盖度行为和 Worker 记忆；删除同步后无调用者的重复默认工具筛选函数 |
| SDK 和模型 | norma 0.3.8、DeepSeek 搜索、空回复续跑 | SDK 以 0.3.4 为基线三方合并到本地 vendor，保留 Windows shell 和重试修复；空回复续跑接入现有 Hook 链 |
| MCP | HTTP 可选忽略 TLS 校验 | 同步开关及导入路径，保留本地连接测试、使用统计和错误分类 |
| 自更新和触发 | 更新页面、启动监督脚本、触发消息去重 | 同步；没有执行实际更新或发布 |
| 数据库和归档 | 证据、复测、旁路问答等新结构 | 合并迁移与恢复白名单；额外保存主会话、复测历史；已不存在的复测会话关联恢复为空，归档中的运行态复测恢复为停止态 |
| UI | 新增证据、复测和问答页面 | 沿用现有 shadcn/Radix 组件，不迁移组件库；任务详情继续动态加载，不恢复整页提前加载 |

## 验证

所有数据库测试使用新建、无数据挂载的 PostgreSQL 16 临时容器，仅映射 `127.0.0.1:55439`。不同包使用独立数据库，未连接或迁移业务数据库。

- Agent、DB、Server、Evidence 完整包测试。
- 其余 Go 包测试，包括 intercept、llmrec、traffic、sidequestion、selfupdate、mcphttp 等。
- 全包编译：`go test -exec=true ./... -run '^$'`。
- 定向 race：Worker pending 执行与 Planner 边界、归档恢复、漏洞证据交接、复测 HTTP/工具流程。
- 本地 SDK：`go test ./agentcore ./tool ./harness ./llm`。
- 前端类型检查、生产构建、活动合并测试、审批模板 Mock 测试。
- 对改动的 39 个 TypeScript 文件定向 Biome 检查；只应用安全修复，没有对全仓应用 unsafe 自动改写。
- 检查暂存与未暂存差异、冲突标记和官方终点的祖先关系。

初次验证发现并修复了：精简 hint 查询漏传流量引用、llmrec 测试缺少使用量表初始化、SDK 重试测试缺少显式 HTTP 客户端。多个包共用一个新测试库时出现迁移死锁，因此最终完整测试按包隔离数据库执行，而不是忽略失败或改动业务库。

## 验证边界

- 未使用真实模型或生产目标执行网络测试，未执行真实自更新；相关行为由自动化测试覆盖。
- 未对生产环境做浏览器验收或吞吐压测，不宣称具体延迟、Token 或性能改善比例。
- Biome 仍有建议级诊断；定向错误级检查通过不代表全仓零警告。
- 保留本地权限模型不意味着过滤器能识别所有自然语言证据中的主机；网络执行仍由独立的运行时检查约束。
